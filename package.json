{
  "name": "shipyard-hello-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}